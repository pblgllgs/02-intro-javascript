const activo = true;

/*let msg = "";

 if (activo) {
  msg = "Activo";
} else {
  msg = "Inactivo";
}

console.log(msg); */

/* const msg = (activo) ? 'Activo' : null; */

const msg = activo && 'Activo';

console.log(msg);
